
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p302@elite-house" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p302@elite-house"
else
  rvm --create use  "ruby-1.8.7-p302@elite-house"
fi
